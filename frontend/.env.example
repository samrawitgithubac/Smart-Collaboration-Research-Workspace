# Production / Render: set to your deployed API (no trailing slash). Required for static hosting.
# VITE_API_URL=https://your-api.onrender.com
# VITE_SOCKET_URL=https://your-api.onrender.com

# Show "Dev bypass login" on sign-in (frontend-only; no API required for that button)
# VITE_AUTH_DEV_BYPASS=true
