DATABASE_URL="postgresql://scrw:scrw@localhost:5432/scrw?schema=public"
JWT_SECRET="generate-a-long-random-string-for-production"
JWT_EXPIRES_IN="7d"
PORT=4000
CORS_ORIGIN="http://localhost:5173"
UPLOAD_DIR="./uploads"
UPLOAD_MAX_MB=25
NODE_ENV=development

# Optional: public API base URL for Swagger "Try it out" (no trailing slash). On Render, RENDER_EXTERNAL_URL is used if unset.
# PUBLIC_API_URL=http://localhost:4000
